<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="160dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:background="@color/white_tag_1" >

        <ImageButton
            android:id="@+id/account_view"
            android:layout_width="fill_parent"
            android:layout_height="45dp"
            android:layout_centerVertical="true"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:gravity="center_vertical"/>

        <EditText
            android:id="@+id/accountEdit"
            android:layout_width="fill_parent"
            android:layout_height="40dp"
            android:layout_alignTop="@id/account_view"
            android:layout_alignBottom="@id/account_view"
            android:layout_alignStart="@id/account_view"
            android:layout_marginLeft="50sp"
            android:hint="注册邮箱"
            android:inputType="textEmailAddress"
            android:ems="10" >
            <requestFocus />
        </EditText>
    </RelativeLayout>


    <TextView
        android:id="@+id/tipText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="20dip"
        android:layout_marginLeft="15dp"
        android:layout_marginRight="15dp"
        android:text="请输入注册时的邮箱账号，系统将通过邮件帮您找回密码"
        android:textColor="#585858"
        android:textSize="15sp" />

    <Button
        android:id="@+id/forgetBtn"
        android:layout_width="fill_parent"
        android:layout_height="40dip"
        android:layout_above="@+id/tipText"
        android:layout_marginBottom="25dp"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:text="密码找回"
        android:textColor="#ffffff"
        android:textSize="20sp" />

</RelativeLayout>